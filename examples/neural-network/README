execute these scripts:

./convertFeatures.sh

converts the .gz files in the feature directory to squirrel caches

./normalize.sh

computes the mean and standard deviation of the data

./train.sh

trains the network

./recog.sh

recognition on the test set, results can be found in log/recognition.log
